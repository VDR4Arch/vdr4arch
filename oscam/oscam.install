post_install() {
  groupadd oscam 2> /dev/null
  useradd -m -d /var/lib/oscam -g oscam -G uucp oscam 2> /dev/null

  chown -R oscam:oscam var/lib/oscam
  chown oscam:oscam /tmp/camd.socket
}

post_upgrade() {
  post_install
}
