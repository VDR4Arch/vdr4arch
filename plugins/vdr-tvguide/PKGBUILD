# Maintainer: Christopher Reimer <vdr4arch[at]creimer[dot]net>
pkgname=vdr-tvguide
_gitrev=9f127fbbb5e0cc508879bb380f214d72db9439e8
pkgver=1.2.0
epoch=1
_vdrapi=2.0.0
pkgrel=1
pkgdesc="highly customizable 2D EPG viewer plugin."
url="http://projects.vdr-developer.org/projects/plg-tvguide"
arch=('x86_64' 'i686')
license=('GPL2')
depends=('graphicsmagick' 'libpng' "vdr-api=$_vdrapi")
makedepends=('git')
install="$pkgname.install"
_plugname=$(echo $pkgname | sed 's/vdr-//g')
source=("git://projects.vdr-developer.org/vdr-plugin-tvguide.git#commit=$_gitrev")
md5sums=('SKIP')

pkgver() {
  cd "${srcdir}/vdr-plugin-${_plugname}"
    git tag -a v1.2.0 -m 'Added Tag' 9f127fbbb5e0cc508879bb380f214d72db9439e8 2> /dev/null
  git describe --tags | sed 's/-/_/g;s/v//'
}

build() {
  cd "$srcdir/vdr-plugin-${_plugname}"
  make IMAGELIB=graphicsmagick
}

package() {
  cd "$srcdir/vdr-plugin-${_plugname}"
  make DESTDIR="$pkgdir" install

#   find "$pkgdir" -name *.png -exec gm convert +profile icm {} {} \;
}
